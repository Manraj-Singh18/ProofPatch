# Agent proof report

Every completed coding-agent run can be materialized as a JSON proof report containing:

- the task;
- attempt count;
- acceptance status;
- claim results and reasons;
- collected evidence;
- the SHA-256 commitment of the verification evidence package.

The report is an output artifact, not an authority by itself. Acceptance remains determined by the independent verification pipeline.
